<%= form_with(model: usuario, local: true) do |form| %>
  <% if usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usuario.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
      <% usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :usuario_nombre %>
  </div>

  <div class="field">
    <%= form.label :cedula %>
    <%= form.number_field :cedula, id: :usuario_cedula %>
  </div>

  <div class="field">
    <%= form.label :tiporeporte %>
    <%= form.text_field :tiporeporte, id: :usuario_tiporeporte %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
